<div class="namespaceSection">
  {{~#each this~}}
    <div id="{{this.id}}" class="namespaceItem" {{#if this.deprecated}}aria-label="deprecated"{{/if}}>
	    {{~> doc_node_kind_icon this.doc_node_kind_ctx ~}}

	    <div class="namespaceItemContent">
		    <a href="{{this.href}}" title="{{this.name}}">
			    <span>{{~this.name~}}</span>
          {{~#if this.ty~}}
            <span>
              {{{~this.ty.ty~}}} {{! typedef rendering }}
            </span>
            {{~#if this.ty.info~}}
              <div class="namespaceItemContentTypeInfo">
                {{~this.ty.info~}}
              </div>
            {{~/if~}}
          {{~/if~}}
        </a>

        {{~#if this.docs~}}
          <div class="namespaceItemContentDoc">
			      {{{~this.docs~}}} {{! markdown rendering }}
          </div>
        {{~/if~}}

        {{~#if this.subitems~}}
          <ul class="namespaceItemContentSubItems">
            {{~#each this.subitems~}}
              <li>
                <a href="{{this.href}}">
                  <span>{{this.title}}</span>
                  {{~#if this.ty~}}
                    <span>
                      {{{~this.ty.ty~}}} {{! typedef rendering }}
                    </span>
                    {{~#if this.ty.info~}}
                      <div class="namespaceItemContentTypeInfo">
                        {{~this.ty.info~}}
                      </div>
                    {{~/if~}}
                  {{~/if~}}
                </a>

                {{~#if this.docs~}}
                  <div class="namespaceItemContentDoc">
                    {{{~this.docs~}}} {{! markdown rendering }}
                  </div>
                {{~/if~}}
              </li>
            {{~/each~}}
          </ul>
        {{~/if~}}
      </div>
    </div>
  {{~/each~}}
</div>
