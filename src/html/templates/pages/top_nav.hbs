<nav class="top-0 sticky bg-white z-50 py-3 h-14">
  <div class="h-full max-lg:flex md:items-center justify-between gap-4 max-md:flex-col-reverse lg:grid lg:grid-cols-4 lg:gap-12">
    <div class="lg:col-span-3">
      {{~> breadcrumbs breadcrumbs_ctx ~}}
    </div>

    {{#if (not disable_search)}}
      <input
        type="text"
        id="searchbar"
        style="display: none;"
        class="lg:col-[span_1_/_-1] flex-1 py-2 px-2.5 rounded text-sm border border-gray-300 bg-transparent" />
    {{/if}}
  </div>
</nav>
