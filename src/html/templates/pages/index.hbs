{{~> pages/html_head html_head_ctx ~}}
{{~> pages/category_panel categories_panel ~}}
<div>
  {{~> pages/top_nav ~}}

  <div id="content">
    <main>
      {{~#if module_doc ~}}
        {{~> module_doc module_doc ~}}
      {{~/if~}}

      {{~#if overview ~}}
        {{~> symbol_content overview ~}}
      {{~/if~}}
    </main>

    {{~> toc toc_ctx ~}}

    {{~> pages/search_results ~}}
  </div>
</div>
</div>
</body>
</html>
